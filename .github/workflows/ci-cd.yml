name: React CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install
        working-directory: ./   # yahan package.json ke folder ka path

      # Test step skipped because no test files exist
      # - name: Run Tests
      #   run: npm test -- --watchAll=false
      #   working-directory: ./   # optional, agar test enable karoge

      - name: Build Application
        run: npm run build
        working-directory: ./   # yahan bhi package.json ka path

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_PAT }}
          publish_dir: ./build
